import {components} from react;
import './discussionboxUser.css';

class discussionboxUser extends components {
    render () {
        return (
            <div className="discussionBox">
                Discussion
            </div>
        )
    }
}